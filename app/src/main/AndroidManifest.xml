<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bitacora20">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bitacora20">
        <activity android:name=".MainActivity"/>

<activity android:name=".activities.InicioActivity">
    <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
    </intent-filter>


</activity>
        <activity android:name=".activities.LoginActivity" >
        </activity>

        <activity android:name=".activities.VerMateriaActivity" >
        </activity>

        <activity android:name=".activities.CrearCuentaActivity" >
        </activity>

        <activity android:name=".activities.CrearMateriaActivity" >
        </activity>

        <activity android:name=".activities.MateriasListView" >
        </activity>

        <activity android:name=".activities.VerDatosMateriaActivity" >
        </activity>

        <activity android:name=".activities.CrearTemaActivity" >
        </activity>

        <activity android:name=".activities.TemasListView" >
        </activity>

        <activity android:name=".activities.VerDatosTemaActivity" >
        </activity>

    </application>

</manifest>